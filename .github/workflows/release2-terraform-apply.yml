name: Release 2 Terraform Apply - Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    name: Deprecated generic multi-root apply disabled
    runs-on: ubuntu-latest

    steps:
      - name: Stop
        run: |
          echo "This generic multi-root apply workflow is disabled."
          echo "Use dedicated root-specific workflows only."
          echo "- release2-governance-apply.yml"
          echo "- release2-platform-shared-apply.yml"
          echo "- release2-platform-networking-hybrid-apply.yml"
          echo "- release2-platform-management-apply.yml"
          echo "- release2-aws-branch-apply.yml"
          exit 1
