apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-audit-agent
  namespace: ai-enclave
  labels:
    azure.workload.identity/use: "true"
    azawslab.phase: O6
    azawslab.o6/agent: "true"
  annotations:
    azure.workload.identity/client-id: "REPLACE_WITH_ID_AI_AUDIT_AGENT_CLIENT_ID"
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-finops-agent
  namespace: ai-enclave
  labels:
    azure.workload.identity/use: "true"
    azawslab.phase: O6
    azawslab.o6/agent: "true"
  annotations:
    azure.workload.identity/client-id: "REPLACE_WITH_ID_AI_FINOPS_AGENT_CLIENT_ID"
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-runbook-agent
  namespace: ai-enclave
  labels:
    azure.workload.identity/use: "true"
    azawslab.phase: O6
    azawslab.o6/agent: "true"
  annotations:
    azure.workload.identity/client-id: "REPLACE_WITH_ID_AI_RUNBOOK_AGENT_CLIENT_ID"
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-gitops-agent
  namespace: ai-enclave
  labels:
    azure.workload.identity/use: "true"
    azawslab.phase: O6
    azawslab.o6/agent: "true"
  annotations:
    azure.workload.identity/client-id: "REPLACE_WITH_ID_AI_GITOPS_AGENT_CLIENT_ID"
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-mcp-gateway
  namespace: ai-enclave
  labels:
    azure.workload.identity/use: "true"
    azawslab.phase: O6
    app.kubernetes.io/name: ai-mcp-gateway
  annotations:
    azure.workload.identity/client-id: "REPLACE_WITH_ID_AI_MCP_GATEWAY_CLIENT_ID"
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-rag-reader
  namespace: ai-enclave
  labels:
    azure.workload.identity/use: "true"
    azawslab.phase: O6
    azawslab.o6/agent: "true"
  annotations:
    azure.workload.identity/client-id: "REPLACE_WITH_ID_AI_RAG_READER_CLIENT_ID"
