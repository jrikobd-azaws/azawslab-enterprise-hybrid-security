---
- name: Webserver role intentionally deferred
  ansible.builtin.debug:
    msg:
      - 'Release 2 webserver role is intentionally deferred.'
      - 'Do not configure IIS or Linux web services until the target host role and application validation scope are confirmed.'
      - 'This placeholder preserves the P2b role scaffold without claiming webserver implementation.'
