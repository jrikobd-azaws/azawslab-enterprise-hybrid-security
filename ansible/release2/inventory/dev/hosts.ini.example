[windows]
vm-dev-client-01 ansible_host=10.10.0.4

[windows:vars]
ansible_user=REDACTED
# ansible_password must be supplied at runtime using Ansible Vault, environment-specific uncommitted inventory, or another approved secret source.
ansible_connection=winrm
ansible_port=5985
ansible_winrm_scheme=http
ansible_winrm_transport=ntlm
ansible_winrm_server_cert_validation=ignore

[fortigate]
fortigate-azure ansible_host=10.0.3.4

[fortigate:vars]
ansible_connection=httpapi
ansible_network_os=fortinet.fortios.fortios
ansible_httpapi_use_ssl=true
ansible_httpapi_validate_certs=false
ansible_httpapi_port=443

# FortiGate token handling:
# Do not commit real tokens.
# Playbooks read the token from the runtime environment:
#   source ~/.azawslab-secrets/fortigate-o1.env
#   FORTIOS_ACCESS_TOKEN='<token value>'
#
# Do not place fortios_access_token or ansible_httpapi_token in committed inventory.
